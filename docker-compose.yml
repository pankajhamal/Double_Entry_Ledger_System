services:
  postgres:
    image: postgres:16
    container_name: ledger_database
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: pankajhamal77
      POSTGRES_DB: ledger_db
    ports:
      - "5432:5432"
    
    volumes:
      - postgres_data:/var/lib/postgresql/ledger_database
  
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@gmail.com
      PGADMIN_DEFAULT_PASSWORD: pankajhamal77
    ports:
      - "5050:80"
    depends_on:
      - postgres
    volumes:
      - pgadmin_data:/var/lib/pgadmin
  redis:
    image: redis:7-alpine
    container_name: ledger_redis
    restart: always
    ports:
    - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
  pgadmin_data: